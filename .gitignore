# Local working files — never published.
# These stay on disk and drive day-to-day work, but they are not part of the
# public artifact and must not be committed.
CLAUDE.md
.claude/
JOURNAL.md
notes/

# C++ model build artifacts
model/build/
*.o
*.d

# Golden vectors: regenerated by reference/gen_vectors.py, not versioned
# (the 'full' set is ~200 KB per tensor and would change on every regeneration)
vectors/*/
!vectors/README.md

# Raw sweep results (the small CSVs are versioned — see sweeps/README.md)
sweeps/raw/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Verilator (Phase 1)
sim/obj_dir/
*.vcd
*.fst

# Vivado (Phase 2)
fpga/*.jou
fpga/*.log
fpga/*.str
fpga/.Xil/
fpga/*.cache/
fpga/*.hw/
fpga/*.ip_user_files/
fpga/*.runs/
fpga/*.sim/
fpga/*.gen/
*.bit
*.hwh
*.xsa

# System
.DS_Store
*.swp
